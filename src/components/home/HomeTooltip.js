import { ColContainer } from '../bubble/Components';
import { Body } from '../carousel/Components';

const HomeTooltip = () => {
  return (
    <ColContainer>
      <Body>템플릿을 꺼내 쓰고, 만들어서 저장할 수 있는</Body>
      <Body>페이지로 이동합니다.</Body>
    </ColContainer>
  );
};

export default HomeTooltip;
